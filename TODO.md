# TODO

## Hero dynamic stats
- [ ] Update `my-app/src/components/Hero.tsx` so "Projects Built" count is fetched from API and replaces hardcoded `8+`.
- [ ] Use `GET {apiBase}/project/` endpoint from backend (`portoAPI/src/index.ts` mounts `/project`).
- [ ] Display loading/fallback value while fetch is in progress.
- [ ] (Optional) Extend same approach for "Technologies" and "Internship Experience" if desired.

